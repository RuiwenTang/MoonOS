#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int kvsnprintf(char* str, size_t size, const char* format, va_list ap) {
    (void)str;
    (void)size;
    (void)format;
    (void)ap;
    return -1;
}